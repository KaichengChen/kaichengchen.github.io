% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/core.R
\name{fit_gmm}
\alias{fit_gmm}
\title{Fit full-sample debiased GMM with multiway clustered inference}
\usage{
fit_gmm(
  data,
  score,
  theta_start,
  cluster_cols,
  nuisance = NULL,
  nuisance_fit = NULL,
  jacobian = NULL,
  weight_type = "PSD",
  covariance_type = "PSD",
  optimizer_method = "BFGS",
  optimizer_options = NULL,
  n_steps = 2L
)
}
\arguments{
\item{data}{A data frame with one row per unit observation.}

\item{score}{Function with arguments `(data, theta, nuisance)`.}

\item{theta_start}{Finite starting parameter vector.}

\item{cluster_cols}{Character vector of clustering variables.}

\item{nuisance}{Precomputed nuisance object.}

\item{nuisance_fit}{Callback fitted exactly once on the complete data.}

\item{jacobian}{Optional analytic negative Jacobian callback.}

\item{weight_type}{`"PSD"` or `"CGM"` for the feasible GMM weight.}

\item{covariance_type}{`"PSD"` or `"CGM"` for reported inference.}

\item{optimizer_method}{Method passed to [stats::optim()].}

\item{optimizer_options}{Control list passed to [stats::optim()].}

\item{n_steps}{Either one or two GMM steps.}
}
\value{
A `fullsampleDML_fit` object.
}
\description{
`score` must return an n-by-q numeric matrix. The optional analytic
`jacobian` must return the q-by-p negative derivative of the average score.
Supply exactly one of a precomputed `nuisance` object or a `nuisance_fit`
callback. Use `nuisance = list()` for nuisance-free models.
}
